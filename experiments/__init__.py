"""
Experiment scripts package.

This file exists so `pytest` and other tooling can reliably import modules like
`experiments.run_ablation` in environments that don't handle namespace
packages consistently.
"""

